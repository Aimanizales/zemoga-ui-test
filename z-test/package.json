{
  "name": "z-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "0.9.x"
  },
  "devDependencies": {
    "node-sass-chokidar": "^0.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build-css": "node-sass-chokidar src/sass/ -o src/css/",
    "watch-css": "npm run build-css && node-sass-chokidar src/sass/ -o src/css/ --watch --recursive",
    "dev": "yarn start && yarn watch-css",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "description": "Zemoga test",
  "main": "index.js",
  "author": "Aiman Abdel Samad",
  "license": "MIT"
}
